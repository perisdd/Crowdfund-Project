@page
@using Crowdfund.DB;
@model Crowdfund.Pages.Users2.Pages_Users2_Index
@{
    ViewData["Title"] = "Users";
}
<h1>Users</h1>
<a class="btn btn-primary" asp-page="Create">Add New</a>

<table class="table">
    <thead>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Details</th>
    </thead>
@*    <tbody>
        @foreach (User user in Model.Backers)
        {
            <tr>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td><a asp-page="Details" asp-route-Id="@user.Id">Details</a></td>
            </tr>
        }
    </tbody>*@
</table>